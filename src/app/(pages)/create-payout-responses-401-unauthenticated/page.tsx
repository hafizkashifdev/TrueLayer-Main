import { CreatePayoutResponses401UnauthenticatedImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const CreatePayoutResponses401UnauthenticatedPage = () => {
  return (
    <CommonPage
      pageTitle={`Create Payout _ Responses_401  Unauthenticated`}
      src={CreatePayoutResponses401UnauthenticatedImage}
      backRoute="/create-payout"
     
    />
  );
};

export default CreatePayoutResponses401UnauthenticatedPage;
