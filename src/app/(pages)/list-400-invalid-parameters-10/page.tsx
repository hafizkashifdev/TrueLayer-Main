import { List400InvalidParameters10Image } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const List400InvalidParameters10Page = () => {
  return (
    <CommonPage
      pageTitle={`400 Invalid Parameters `}
      src={List400InvalidParameters10Image}
      backRoute="/mandates-list-mandates"
    />
  );
};

export default List400InvalidParameters10Page;
