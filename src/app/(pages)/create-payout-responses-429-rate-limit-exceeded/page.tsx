import { CreatePayoutResponses429RateLimitExceededImage } from "@/assets";
import { CommonPage } from "@/components";

import React from "react";

const CreatePayoutResponses429RateLimitExceededPage = () => {
  return (
    <CommonPage
      pageTitle={` Create Payout _ Responses_429  Rate Limit Exceeded`}
      src={CreatePayoutResponses429RateLimitExceededImage}
      backRoute="/create-payout"
    
    />
  );
};

export default CreatePayoutResponses429RateLimitExceededPage;
