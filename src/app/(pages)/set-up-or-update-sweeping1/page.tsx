import { SetUpOrUpdateSweeping1Image } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const SetUpOrUpdateSweeping1Page = () => {
  return (
    <CommonPage
      pageTitle={`Set up or update sweeping`}
      src={SetUpOrUpdateSweeping1Image}
      backRoute="/Set up or update sweeping"
    />
  );
};

export default SetUpOrUpdateSweeping1Page;
