import { ResponseBodyMetadata08Image } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const ResponseBodyMetadata08Page = () => {
  return (
    <CommonPage
      pageTitle={`Response Body`}
      src={ResponseBodyMetadata08Image}
      backRoute="/meta-main"
    />
  );
};

export default ResponseBodyMetadata08Page;
