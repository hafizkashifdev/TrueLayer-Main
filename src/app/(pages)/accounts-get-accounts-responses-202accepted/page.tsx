import { AccountsGetAccountsResponses202AcceptedImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const AccountsGetAccountsResponses202AcceptedPage = () => {
  return (
    <CommonPage
      pageTitle={`202(Accepted))`}
      src={AccountsGetAccountsResponses202AcceptedImage}
      backRoute="/accounts-get-accounts"
    />
  );
};

export default AccountsGetAccountsResponses202AcceptedPage;
