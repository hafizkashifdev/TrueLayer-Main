import { AccountsGetAccountPending202AcceptedImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const AccountsGetAccountPending202AcceptedPage = () => {
  return (
    <CommonPage
      pageTitle={`Accounts Get account pending 202 accepted`}
      src={AccountsGetAccountPending202AcceptedImage}
      backRoute="/account-get-account-pending-transaction"
    />
  );
};

export default AccountsGetAccountPending202AcceptedPage;
