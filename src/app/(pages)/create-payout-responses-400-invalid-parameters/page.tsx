import { CreatePayoutResponses400InvalidParametersImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const CreatePayoutResponses400InvalidParametersPage = () => {
  return (
    <CommonPage
      pageTitle={` Create Payout _ Responses_400 Invalid Parameters`}
      src={CreatePayoutResponses400InvalidParametersImage}
      backRoute="/create-payout"
    />
  );
};

export default CreatePayoutResponses400InvalidParametersPage;
