import { SubmitConsentResponses400InvalidRequestImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const SubmitConsentResponses400InvalidRequestPage = () => {
  return (
    <CommonPage
      pageTitle={`Submit consent _ Responses_400 Invalid Request`}
      src={SubmitConsentResponses400InvalidRequestImage}
      backRoute="/payments-submit-consent"
    />
  );
};

export default SubmitConsentResponses400InvalidRequestPage;
