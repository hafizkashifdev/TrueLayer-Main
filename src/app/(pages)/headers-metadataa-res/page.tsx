import { HeadersMetadataaResImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const HeadersMetadataaResPage = () => {
  return (
    <CommonPage
      pageTitle={`Headers metadataa-res`}
      src={HeadersMetadataaResImage}
      backRoute="/meta-main"
    />
  );
};

export default HeadersMetadataaResPage;
