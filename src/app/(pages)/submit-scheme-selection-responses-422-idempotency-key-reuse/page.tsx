import { SubmitSchemeSelectionResponses422IdempotencyKeyReuseImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const SubmitSchemeSelectionResponses422IdempotencyKeyReusePage = () => {
  return (
    <CommonPage
      pageTitle={`Submit scheme selection_ Responses_422 Idempotency-Key Reuse`}
      src={SubmitSchemeSelectionResponses422IdempotencyKeyReuseImage}
      backRoute="/payments-submit-scheme-selection"
    />
  );
};

export default SubmitSchemeSelectionResponses422IdempotencyKeyReusePage;
