import { SubmitConsentBodyParamsImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const SubmitConsentBodyParamsPage = () => {
  return (
    <CommonPage
      pageTitle={`Submit consent_ Body  Params`}
      src={SubmitConsentBodyParamsImage}
      backRoute="/payments-submit-consent"
    />
  );
};

export default SubmitConsentBodyParamsPage;
