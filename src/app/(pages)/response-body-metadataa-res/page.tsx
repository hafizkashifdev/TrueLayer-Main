import { ResponseBodyMetadataaResImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const ResponseBodyMetadataaResPage = () => {
  return (
    <CommonPage
      pageTitle={`Response Body`}
      src={ResponseBodyMetadataaResImage}
      backRoute="/meta-main"
    />
  );
};

export default ResponseBodyMetadataaResPage;
