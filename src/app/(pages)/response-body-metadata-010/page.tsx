import { ResponseBodyMetadata010Image } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const ResponseBodyMetadata010Page = () => {
  return (
    <CommonPage
      pageTitle={`Response Body`}
      src={ResponseBodyMetadata010Image}
      backRoute="/meta-main"
    />
  );
};

export default ResponseBodyMetadata010Page;
