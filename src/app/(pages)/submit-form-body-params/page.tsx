import { SubmitFormBodyParamsImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const SubmitFormBodyParamsPage = () => {
  return (
    <CommonPage
      pageTitle={`Submit form_ Body  Params`}
      src={SubmitFormBodyParamsImage}
      backRoute="/payments-submit-form"
    />
  );
};

export default SubmitFormBodyParamsPage;
