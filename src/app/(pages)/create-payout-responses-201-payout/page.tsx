import { CreatePayoutResponses201PayoutImage } from "@/assets";
import { CommonPage } from "@/components";

import React from "react";

const CreatePayoutResponses201PayoutPage = () => {
  return (
    <CommonPage
      pageTitle={`Mental Health / Mental Health  Act 1983 / Part V / Create Payout _ Responses_201 Payout`}
      src={CreatePayoutResponses201PayoutImage}
      backRoute="/create-payout"
    />
  );
};

export default CreatePayoutResponses201PayoutPage;
