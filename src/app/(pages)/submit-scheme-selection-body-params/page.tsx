import { SubmitSchemeSelectionBodyParamsImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const SubmitSchemeSelectionBodyParamsPage = () => {
  return (
    <CommonPage
      pageTitle={`Submit scheme selection_ Body  Params`}
      src={SubmitSchemeSelectionBodyParamsImage}
      backRoute="/payments-submit-scheme-selection"
    />
  );
};

export default SubmitSchemeSelectionBodyParamsPage;
