import { SubmitSchemeSelectionResponses400InvalidRequestImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const SubmitSchemeSelectionResponses400InvalidRequestPage = () => {
  return (
    <CommonPage
      pageTitle={`Submit scheme selection_ Responses_400 Invalid Request`}
      src={SubmitSchemeSelectionResponses400InvalidRequestImage}
      backRoute="/payments-submit-scheme-selection"
    />
  );
};

export default SubmitSchemeSelectionResponses400InvalidRequestPage;
