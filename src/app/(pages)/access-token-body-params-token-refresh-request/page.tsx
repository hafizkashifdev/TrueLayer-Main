import { AccessTokenBodyParamsTokenRefreshRequestImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const AccessTokenBodyParamsTokenRefreshRequestPage = () => {
  return (
    <CommonPage
      pageTitle={`Access token _ Body Params_ Token Refresh Request`}
      src={AccessTokenBodyParamsTokenRefreshRequestImage}
      backRoute="/authentication-server-access-token"
    />
  );
};

export default AccessTokenBodyParamsTokenRefreshRequestPage;
