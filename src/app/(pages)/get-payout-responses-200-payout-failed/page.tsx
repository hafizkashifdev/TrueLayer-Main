import { GetPayoutResponses200PayoutFailedImage } from "@/assets";

import AmendmentComparison from "@/components/amendment-comparison";
import React from "react";

const GetPayoutResponses200PayoutFailedPage = () => {
  return (
    <AmendmentComparison
      pageTitle={`Mental Health / Mental Health  Act 1983 / Part V / Get Payout _ Responses_200 Payout_Failed`}
      src={GetPayoutResponses200PayoutFailedImage}
      backRoute="/"
      amendmentButtonRoute="/get-payout"
    />
  );
};

export default GetPayoutResponses200PayoutFailedPage;
