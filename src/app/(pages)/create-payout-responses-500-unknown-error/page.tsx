import { CreatePayoutResponses500UnknownErrorImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const CreatePayoutResponses500UnknownErrorPage = () => {
  return (
    <CommonPage
      pageTitle={` Create Payout _ Responses_500 Unknown Error`}
      src={CreatePayoutResponses500UnknownErrorImage}
      backRoute="/create-payout"
    />
  );
};

export default CreatePayoutResponses500UnknownErrorPage;
