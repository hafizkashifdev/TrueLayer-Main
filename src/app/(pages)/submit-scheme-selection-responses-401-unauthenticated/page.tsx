import { SubmitSchemeSelectionResponses401UnauthenticatedImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const SubmitSchemeSelectionResponses401UnauthenticatedPage = () => {
  return (
    <CommonPage
      pageTitle={`Submit scheme selection_ Responses_401 Unauthenticated`}
      src={SubmitSchemeSelectionResponses401UnauthenticatedImage}
      backRoute="/payments-submit-scheme-selection"
    />
  );
};

export default SubmitSchemeSelectionResponses401UnauthenticatedPage;
