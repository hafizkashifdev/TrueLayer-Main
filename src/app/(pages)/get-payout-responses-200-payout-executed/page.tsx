import { GetPayoutResponses200PayoutExecutedImage } from "@/assets";

import AmendmentComparison from "@/components/amendment-comparison";
import React from "react";

const GetPayoutResponses200PayoutExecutedPage = () => {
  return (
    <AmendmentComparison
      pageTitle={`Mental Health / Mental Health  Act 1983 / Part V / Get Payout _ Responses_200 Payout_Executed`}
      src={GetPayoutResponses200PayoutExecutedImage}
      backRoute="/"
      amendmentButtonRoute="/get-payout"
    />
  );
};

export default GetPayoutResponses200PayoutExecutedPage;
