import { HeadersMetadata08Image } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const HeadersMetadata010Page = () => {
  return (
    <CommonPage
      pageTitle={`Headers`}
      src={HeadersMetadata08Image}
      backRoute="/meta-main"
    />
  );
};

export default HeadersMetadata010Page;
